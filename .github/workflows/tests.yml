name: Run Tests

on:
  pull_request:
  push:
    branches:
      - update

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run dummy test
        run: echo "Checkout Code thành công."
